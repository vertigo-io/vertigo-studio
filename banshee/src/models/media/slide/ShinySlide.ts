/**
 * ShinySlide is an interface representing a slide component within the Banshee frontend.
 * It is used to display presentation slides defined using Markdown content.
 */
export interface ShinySlide {
    /**
     * The Markdown content of the slides.
     */
    markdown: string;
}
